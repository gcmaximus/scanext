rules:
  - id: chrome_tabs_get-chrome_tabs_update
    languages: [javascript]
    message: "Source:chrome_tabs_get;Sink:chrome_tabs_update"
    mode: taint
    pattern-sources:
      - pattern: $TABS = await chrome.tabs.get(...)
      - patterns:
          - pattern: |
              function $FUNC($TABS) {
                ...
              }
              ...
              chrome.tabs.get(..., $FUNC)
          - focus-metavariable:
              - $TABS
      - patterns:
          - pattern: |
              chrome.tabs.get(..., $FUNC)
              ...
              function $FUNC($TABS) {
                ...
              }
          - focus-metavariable:
              - $TABS
      - patterns:
          - pattern: |
              chrome.tabs.get(..., function ($TABS) {
                ...
              })
          - focus-metavariable:
              - $TABS
      - patterns:
          - pattern-inside: |
              chrome.tabs.get(..., function ([$TAB]) {
                ...
              })
          - pattern: $TAB
          - focus-metavariable:
              - $TAB
      - patterns:
          - pattern: |
              chrome.tabs.get(...).then(function ($TABS) {
                ...
              })
          - focus-metavariable:
              - $TABS
      - patterns:
          - pattern-inside: |
              chrome.tabs.get(...).then(function ([$TAB]) {
                ...
              })
          - pattern: $TAB
          - focus-metavariable:
              - $TAB
      - patterns:
          - pattern: |
              async function $FUNC(...) {
                ...
                $Y = await chrome.tabs.get(...)
                ...
                return <... $Y ...>
              }
              ...
              $FUNC(...).then(function ($TABS) {...})
          - focus-metavariable:
              - $TABS
      - patterns:
          - pattern-inside: |
              async function $FUNC(...) {
                ...
                $Y = await chrome.tabs.get(...)
                ...
                return $Y
              }
              ...
              $FUNC(...).then(function ([$TAB]) {...})
          - pattern: $TAB
          - focus-metavariable:
              - $TAB
      - patterns:
          - pattern: |
              async function $FUNC(...) {
                ...
                [$Y] = await chrome.tabs.get(...)
                ...
                return <... $Y ...>
              }
              ...
              $FUNC(...).then(function ($TAB) {...})
          - focus-metavariable:
              - $TAB
      - patterns:
          - pattern: |
              $Y = chrome.tabs.get(...)
              ...
              $Y.then(function ($TABS) {...})
    pattern-sinks:
      - patterns:
          - pattern: |
              chrome.tabs.update(..., {url: $X}, ...)
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $X = {url:$Y}
              ...
              chrome.tabs.update($X, ...)
          - focus-metavariable:
              - $Y
      - patterns:
          - pattern: |
              $X = {url: $Y}
              ...
              chrome.tabs.update($W, $X, ...)
          - focus-metavariable:
              - $Y
    severity: WARNING