rules:
  - id: chrome_tabs_get-jQuery_appendTo
    languages: [javascript]
    message: "Source:chrome_tabs_get;Sink:jQuery_appendTo"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.get($TAB_ID)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).appendTo(...)"
        - focus-metavariable: $X
    severity: WARNING
