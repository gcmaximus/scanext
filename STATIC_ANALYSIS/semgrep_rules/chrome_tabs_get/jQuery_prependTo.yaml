rules:
  - id: chrome_tabs_get-jQuery_prependTo
    languages: [javascript]
    message: "Source:chrome_tabs_get;Sink:jQuery_prependTo"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.get($TAB_ID)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).prependTo(...)"
        - focus-metavariable: $X
    severity: WARNING
