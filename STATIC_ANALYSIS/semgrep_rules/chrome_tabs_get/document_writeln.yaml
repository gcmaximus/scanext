rules:
  - id: chrome_tabs_get-document_writeln
    languages: [javascript]
    message: "Source:chrome_tabs_get;Sink:document_writeln"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.get($TAB_ID)
    pattern-sinks:
      - pattern: "document.writeln($X)"
      - patterns:
        - pattern: "... .document.writeln($X)"
        - focus-metavariable: $X
    severity: WARNING