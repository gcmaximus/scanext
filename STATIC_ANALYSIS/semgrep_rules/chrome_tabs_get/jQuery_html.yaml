rules:
  - id: chrome_tabs_get-jQuery_html
    languages: [javascript]
    message: "Source:chrome_tabs_get;Sink:jQuery_html"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.get($TAB_ID)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).html($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.html($X)
        - focus-metavariable: $X
    severity: WARNING