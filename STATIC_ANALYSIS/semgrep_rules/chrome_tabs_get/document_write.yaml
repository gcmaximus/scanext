rules:
  - id: chrome_tabs_get-document_write
    languages: [javascript]
    message: "Source:chrome_tabs_get;Sink:document_write"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.get($TAB_ID)
    pattern-sinks:
      - pattern: "document.write($X)"
      - patterns:
        - pattern: "... .document.write($X)"
        - focus-metavariable: $X
    severity: WARNING