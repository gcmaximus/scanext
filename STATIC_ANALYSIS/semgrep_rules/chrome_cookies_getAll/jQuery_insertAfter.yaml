rules:
  - id: chrome_cookies_getAll-jQuery_insertAfter
    languages:
      - javascript
    message: Source:chrome_cookies_getAll;Sink:jQuery_insertAfter
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern-inside: |
                  function $FUNC($X, ...) {
                    ...
                  }
                  ...
                  chrome.cookies.getAll($DETAILS, $FUNC)
              - pattern-inside: |
                  chrome.cookies.getAll($DETAILS, $FUNC)
                  ...
                  function $FUNC($X, ...) {
                    ...
                  }
              - pattern-inside: |
                  chrome.cookies.getAll($DETAILS, function ($X, ...) {
                    ...
                  })
          - pattern-either:
              - pattern: $X.name
              - pattern: $X.value
    pattern-sinks:
      - patterns:
          - pattern: $($X).insertAfter(...)
          - focus-metavariable: $X
    severity: WARNING
