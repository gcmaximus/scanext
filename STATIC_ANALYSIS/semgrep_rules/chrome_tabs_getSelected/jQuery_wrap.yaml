rules:
  - id: chrome_tabs_getSelected-jQuery_wrap
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:jQuery_wrap"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).wrap($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.wrap($X)
        - focus-metavariable: $X
    severity: WARNING
