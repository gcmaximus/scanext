rules:
  - id: chrome_tabs_getSelected-jQuery_insertAfter
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:jQuery_insertAfter"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected(...)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).insertAfter(...)"
        - focus-metavariable: $X
    severity: WARNING
