rules:
  - id: chrome_tabs_getSelected-jQuery_append
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:jQuery_append"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).append($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.append($X)
        - focus-metavariable: $X
    severity: WARNING
