rules:
  - id: chrome_tabs_getSelected-jQuery_insertBefore
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:jQuery_insertBefore"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected($WINDOW_ID)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).insertBefore(...)"
        - focus-metavariable: $X
    severity: WARNING
