rules:
  - id: chrome_tabs_getSelected-jQuery_wrapInner
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:jQuery_wrapInner"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected($WINDOW_ID)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).wrapInner($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.wrapInner($X)
        - focus-metavariable: $X
    severity: WARNING
