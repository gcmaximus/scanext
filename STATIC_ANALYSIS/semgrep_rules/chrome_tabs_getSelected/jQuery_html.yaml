rules:
  - id: chrome_tabs_getSelected-jQuery_html
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:jQuery_html"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected($WINDOW_ID)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).html($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.html($X)
        - focus-metavariable: $X
    severity: WARNING