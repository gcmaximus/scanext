rules:
  - id: chrome_tabs_getSelected-jQuery_before
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:jQuery_before"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).before($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.before($X)
        - focus-metavariable: $X
    severity: WARNING
