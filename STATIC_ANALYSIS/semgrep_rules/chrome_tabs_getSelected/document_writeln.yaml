rules:
  - id: chrome_tabs_getSelected-document_writeln
    languages: [javascript]
    message: "Source:chrome_tabs_getSelected;Sink:document_writeln"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getSelected($WINDOW_ID)
    pattern-sinks:
      - pattern: "document.writeln($X)"
      - patterns:
        - pattern: "... .document.writeln($X)"
        - focus-metavariable: $X
    severity: WARNING