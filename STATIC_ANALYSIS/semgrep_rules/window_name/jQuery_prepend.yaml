rules:
  - id: window_name-jQuery_prepend
    languages: [javascript]
    message: "Source:window.name;Sink:jQuery.prepend"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - pattern: "$($X).prepend(...)"
    severity: WARNING
