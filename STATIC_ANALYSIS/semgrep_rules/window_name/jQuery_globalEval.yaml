rules:
  - id: window_name-jQuery_globalEval
    languages: [javascript]
    message: "Source:window.name;Sink:jQuery.globalEval"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - patterns:
        - pattern: "jQuery.globalEval($X)"
        - focus-metavariable: $X
    severity: WARNING