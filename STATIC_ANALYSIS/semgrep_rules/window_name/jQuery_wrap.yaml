rules:
  - id: window_name-jQuery_wrap
    languages: [javascript]
    message: "Source:window.name;Sink:jQuery.wrap"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - pattern: "$($X).wrap(...)"
    severity: WARNING
