rules:
  - id: window_name-outerHTML
    languages: [javascript]
    message: "Source:window_name;Sink:outerHTML"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - patterns:
          - pattern: document. ... .outerHTML = $X
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = document. ...
              ...
              $Y. ... .outerHTML = $X
          - focus-metavariable: $X
    severity: WARNING