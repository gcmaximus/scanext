rules:
  - id: window_name-jQuery_append
    languages: [javascript]
    message: "Source:window.name;Sink:jQuery.append"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - pattern: "$($X).append(...)"
    severity: WARNING
