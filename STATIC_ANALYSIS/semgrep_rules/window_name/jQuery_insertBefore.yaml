rules:
  - id: window_name-jQuery_insertBefore
    languages: [javascript]
    message: "Source:window.name;Sink:jQuery.insertBefore"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - pattern: "$($X).insertBefore(...)"
    severity: WARNING
