rules:
  - id: window_name-setAttribute_src
    languages: [javascript]
    message: "Source:window.name;Sink:setAttribute('src')"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - patterns:
        - pattern: "$OBJECT.setAttribute('src', <... $X ...>)"
        - focus-metavariable: $X
    severity: WARNING
