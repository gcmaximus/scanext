rules:
  - id: window_name-jQuery_html
    languages: [javascript]
    message: "Source:window.name;Sink:jQuery.html"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - pattern: "$($X).html(...)"
    severity: WARNING