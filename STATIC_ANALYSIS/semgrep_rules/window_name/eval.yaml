rules:
  - id: window_name-eval
    languages: [javascript]
    message: "Source:window.name;Sink:eval"
    mode: taint
    pattern-sources:
      - pattern: window.name
      - pattern: $X = window.name
    pattern-sinks:
      - pattern: "eval(...)"
    severity: WARNING