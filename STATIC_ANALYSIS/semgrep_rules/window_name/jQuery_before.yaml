rules:
  - id: window_name-jQuery_before
    languages: [javascript]
    message: "Source:window.name;Sink:jQuery.before"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - pattern: $OBJECT.before(...)
    severity: WARNING
