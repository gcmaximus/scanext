rules:
  - id: location_hash-innerHTML
    languages: [javascript]
    message: "Source:location.hash;Sink:innerHTML"
    mode: taint
    pattern-sources:
      - pattern: location.hash
    pattern-sinks:
      - patterns:
        - pattern: "$OBJECT.innerHTML = <... $X ...>"
        - focus-metavariable: $X
    severity: WARNING