rules:
  - id: location_hash-jQuery_insertBefore
    languages: [javascript]
    message: "Source:location.hash;Sink:jQuery.insertBefore"
    mode: taint
    pattern-sources:
      - pattern: location.hash
    pattern-sinks:
      - patterns:
        - pattern: "$($X).insertBefore(...)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $($X).insertBefore($Y)
        - focus-metavariable: $X
    severity: WARNING
