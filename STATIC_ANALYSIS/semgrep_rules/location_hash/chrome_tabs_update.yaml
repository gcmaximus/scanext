rules:
  - id: location_hash-chrome_tabs_update
    languages: [javascript]
    message: "Source:location.hash;Sink:chrome.tabs.update"
    mode: taint
    pattern-sources:
      - pattern: location.hash
    pattern-sinks:
      - patterns:
        - pattern: "chrome.tabs.update({ ..., url: $X })"
        - focus-metavariable: $X
    severity: WARNING