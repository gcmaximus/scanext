rules:
  - id: location_hash-jQuery_appendTo
    languages: [javascript]
    message: "Source:location.hash;Sink:jQuery.appendTo"
    mode: taint
    pattern-sources:
      - pattern: location.hash
      - pattern: $X = location.hash
    pattern-sinks:
      - pattern: "$(<... location.hash ...>).appendTo(...)"
      - pattern: "$(<... $X ...>).appendTo(...)"
    severity: WARNING
