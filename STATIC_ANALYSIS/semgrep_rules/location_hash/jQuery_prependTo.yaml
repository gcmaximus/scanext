rules:
  - id: location_hash-jQuery_prependTo
    languages: [javascript]
    message: "Source:location.hash;Sink:jQuery.prependTo"
    mode: taint
    pattern-sources:
      - pattern: location.hash
    pattern-sinks:
      - patterns:
        - pattern: "$($X).prependTo(...)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $($X).prependTo($Y)
        - focus-metavariable: $X
    severity: WARNING
