rules:
  - id: location_hash-jQuery_prependTo
    languages: [javascript]
    message: "Source:location.hash;Sink:jQuery.prependTo"
    mode: taint
    pattern-sources:
      - pattern: location.hash
      - pattern: $X = location.hash
    pattern-sinks:
      - pattern: "$(<... location.hash ...>).prependTo(...)"
      - pattern: "$(<... $X ...>).prependTo(...)"
    severity: WARNING
