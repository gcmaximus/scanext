rules:
  - id: chrome_tabs_getAllInWindow-jQuery_append
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_append"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).append($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.append($X)
        - focus-metavariable: $X
    severity: WARNING
