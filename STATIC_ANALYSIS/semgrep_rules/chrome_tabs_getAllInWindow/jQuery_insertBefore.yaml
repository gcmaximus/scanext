rules:
  - id: chrome_tabs_getAllInWindow-jQuery_insertBefore
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_insertBefore"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).insertBefore(...)"
        - focus-metavariable: $X
    severity: WARNING
