rules:
  - id: chrome_tabs_getAllInWindow-jQuery_appendTo
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_appendTo"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).appendTo(...)"
        - focus-metavariable: $X
    severity: WARNING
