rules:
  - id: chrome_tabs_getAllInWindow-jQuery_html
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_html"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).html($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.html($X)
        - focus-metavariable: $X
    severity: WARNING