rules:
  - id: chrome_tabs_getAllInWindow-jQuery_before
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_before"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).before($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.before($X)
        - focus-metavariable: $X
    severity: WARNING
