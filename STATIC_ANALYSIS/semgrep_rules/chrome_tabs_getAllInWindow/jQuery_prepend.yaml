rules:
  - id: chrome_tabs_getAllInWindow-jQuery_prepend
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_prepend"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).prepend($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.prepend($X)
        - focus-metavariable: $X
    severity: WARNING
