rules:
  - id: chrome_tabs_getAllInWindow-jQuery_insertAfter
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_insertAfter"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).insertAfter(...)"
        - focus-metavariable: $X
    severity: WARNING
