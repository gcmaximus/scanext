rules:
  - id: chrome_tabs_getAllInWindow-jQuery_after
    languages: [javascript]
    message: "Source:chrome_tabs_getAllInWindow;Sink:jQuery_after"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getAllInWindow(...)
    pattern-sinks:
      - patterns:
        - pattern: "$(...).after($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.after($X)
        - focus-metavariable: $X
    severity: WARNING