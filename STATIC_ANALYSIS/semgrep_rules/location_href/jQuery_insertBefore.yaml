rules:
  - id: location_href-jQuery_insertBefore
    languages: [javascript]
    message: "Source:location.href;Sink:jQuery.insertBefore"
    mode: taint
    pattern-sources:
      - pattern: location.href
    pattern-sinks:
      - patterns:
        - pattern: "$($X).insertBefore(...)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $($X).insertBefore($Y)
        - focus-metavariable: $X
    severity: WARNING
