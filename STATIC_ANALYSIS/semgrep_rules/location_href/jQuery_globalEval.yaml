rules:
  - id: location_href-jQuery_globalEval
    languages: [javascript]
    message: "Source:location.href;Sink:jQuery.globalEval"
    mode: taint
    pattern-sources:
      - pattern: location.href
    pattern-sinks:
      - patterns:
        - pattern: "jQuery.globalEval($X)"
        - focus-metavariable: $X
    severity: WARNING