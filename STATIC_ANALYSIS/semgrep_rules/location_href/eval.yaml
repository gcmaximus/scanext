rules:
  - id: location_href-eval
    languages: [javascript]
    message: "Source:location.href;Sink:eval"
    mode: taint
    pattern-sources:
      - pattern: location.href
    pattern-sinks:
      - patterns:
        - pattern: "eval($X)"
        - focus-metavariable: $X
    severity: WARNING