rules:
  - id: location_href-jQuery_prependTo
    languages: [javascript]
    message: "Source:location.href;Sink:jQuery.prependTo"
    mode: taint
    pattern-sources:
      - pattern: location.href
    pattern-sinks:
      - patterns:
        - pattern: "$($X).prependTo(...)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $($X).prependTo($Y)
        - focus-metavariable: $X
    severity: WARNING
