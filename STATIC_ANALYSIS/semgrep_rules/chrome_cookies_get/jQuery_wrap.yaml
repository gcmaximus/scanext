rules:
  - id: chrome_cookies_get-jQuery_wrap
    languages:
      - javascript
    message: Source:chrome_cookies_get;Sink:jQuery_wrap
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern-inside: |
                  function $FUNC($COOKIE, ...) {
                    ...
                  }
                  ...
                  chrome.cookies.get($DETAILS, $FUNC)
              - pattern-inside: |
                  chrome.cookies.get($DETAILS, $FUNC)
                  ...
                  function $FUNC($COOKIE, ...) {
                    ...
                  }
              - pattern-inside: |
                  chrome.cookies.get($DETAILS, function ($COOKIE, ...) {
                    ...
                  })
              - pattern-inside: |
                  chrome.cookies.get($DETAILS, function $FUNC($COOKIE, ...) {
                    ...
                  })
          - pattern-either:
              - pattern: $COOKIE.name
              - pattern: $COOKIE.value
    pattern-sinks:
      - patterns:
          - pattern: $(...).wrap($X)
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = $(...)
              ...
              $Y.wrap($X)
          - focus-metavariable: $X
    severity: WARNING
