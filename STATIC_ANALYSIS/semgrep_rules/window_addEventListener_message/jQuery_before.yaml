rules:
  - id: chrome_runtime_onConnect-jQuery_before
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  window.addEventListener($MESSAGE, function($X){
                  ...  
                  })  
              - pattern: |
                  function $CALLBACK($X,...){
                    ...
                  }
                  ...
                  window.addEventListener($MESSAGE,$ANY)
              - pattern: |
                  window.addEventListener($MESSAGE,$ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern: $(...).before($X)
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = $(...)
              ...
              $Y.before($X)
          - focus-metavariable: $X
    message: Source:chrome_runtime_onConnect;Sink:jQuery_before
    languages:
      - js
    severity: WARNING
