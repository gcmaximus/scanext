rules:
  - id: chrome_runtime_onConnect-jQuery_appendTo
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  window.addEventListener("message", function($X){
                  ...  
                  })  
              - pattern: |
                  function $ANY(event){
                    ...
                  }
                  ...
                  window.addEventListener("message",$ANY)
              - pattern: |
                  window.addEventListener("message",$ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - patterns:
          - pattern: "$($X).appendTo(...)"
          - focus-metavariable: $X
    message: Source:chrome_runtime_onConnect;Sink:jQuery_appendTo
    languages:
      - js
    severity: WARNING
