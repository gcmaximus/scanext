rules:
  - id: chrome_runtime_onConnect-document_write
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  window.addEventListener("message", function($X){
                  ...  
                  })  
              - pattern: |
                  function $ANY(event){
                    ...
                  }
                  ...
                  window.addEventListener("message",$ANY)
              - pattern: |
                  window.addEventListener("message",$ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - pattern: document.write($X)
      - patterns:
          - pattern: ... .document.write($X)
          - focus-metavariable: $X
    message: Source:chrome_runtime_onConnect;Sink:document_write
    languages:
      - js
    severity: WARNING