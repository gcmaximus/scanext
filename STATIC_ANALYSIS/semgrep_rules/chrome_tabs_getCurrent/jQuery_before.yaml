rules:
  - id: chrome_tabs_getCurrent-jQuery_before
    languages: [javascript]
    message: "Source:chrome_tabs_getCurrent;Sink:jQuery_before"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getCurrent()
    pattern-sinks:
      - patterns:
        - pattern: "$(...).before($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.before($X)
        - focus-metavariable: $X
    severity: WARNING
