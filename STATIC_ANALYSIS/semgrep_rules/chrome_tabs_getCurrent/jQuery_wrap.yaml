rules:
  - id: chrome_tabs_getCurrent-jQuery_wrap
    languages: [javascript]
    message: "Source:chrome_tabs_getCurrent;Sink:jQuery_wrap"
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
            - pattern-inside: await chrome.tabs.getCurrent()
            - pattern-inside: |
                function $FUNC($TABS) {
                  ...
                }
                ...
                chrome.tabs.getCurrent($FUNC)
            - pattern-inside: |
                chrome.tabs.getCurrent($FUNC)
                ...
                function $FUNC($TABS) {
                  ...
                }
            - pattern-inside: |
                chrome.tabs.getCurrent(function ($TABS) {
                  ...
                })
            - pattern-inside: |
                chrome.tabs.getCurrent(function ([$TABS]) {
                  ...
                })
            - pattern-inside: |
                chrome.tabs.getCurrent().then(function ($TABS) {
                  ...
                })
            - pattern-inside: |
                chrome.tabs.getCurrent().then(function ([$TABS]) {
                  ...
                })
            - pattern-inside: |
                async function $FUNC(...) {
                  ...
                  $Y = await chrome.tabs.getCurrent()
                  ...
                  return $Y
                }
                ...
                $FUNC(...).then(function ($TABS) {...})
            - pattern-inside: |
                async function $FUNC(...) {
                  ...
                  $Y = await chrome.tabs.getCurrent()
                  ...
                  return $Y
                }
                ...
                $FUNC(...).then(function ([$TABS]) {...})
            - pattern-inside: |
                async function $FUNC(...) {
                  ...
                  [$Y] = await chrome.tabs.getCurrent()
                  ...
                  return $Y
                }
                ...
                $FUNC(...).then(function ($TABS) {...})
            - pattern-inside: |
                $Y = chrome.tabs.getCurrent()
                ...
                $Y.then(function ($TABS) {...})
            - pattern-inside: |
                $Y = chrome.tabs.getCurrent()
                ...
                $Y.then(function ([$TABS]) {...})
          - pattern: $TABS
    pattern-sinks:
      - patterns:
        - pattern: "$(...).wrap($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.wrap($X)
        - focus-metavariable: $X
    severity: WARNING
