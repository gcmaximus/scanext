rules:
  - id: chrome_tabs_getCurrent-jQuery_appendTo
    languages: [javascript]
    message: "Source:chrome_tabs_getCurrent;Sink:jQuery_appendTo"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getCurrent()
    pattern-sinks:
      - patterns:
        - pattern: "$($X).appendTo(...)"
        - focus-metavariable: $X
    severity: WARNING
