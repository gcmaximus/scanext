rules:
  - id: chrome_tabs_getCurrent-jQuery_wrapInner
    languages: [javascript]
    message: "Source:chrome_tabs_getCurrent;Sink:jQuery_wrapInner"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.getCurrent()
    pattern-sinks:
      - patterns:
        - pattern: "$(...).wrapInner($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.wrapInner($X)
        - focus-metavariable: $X
    severity: WARNING
