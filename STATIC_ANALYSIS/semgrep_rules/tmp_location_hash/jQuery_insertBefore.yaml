rules:
  - id: location_hash-jQuery_insertBefore
    languages: [javascript]
    message: "Source:location.hash;Sink:jQuery.insertBefore"
    mode: taint
    pattern-sources:
      - pattern: location.hash
      - pattern: $X = location.hash
    pattern-sinks:
      - pattern: "$(<... location.hash ...>).insertBefore(...)"
      - pattern: "$($X).insertBefore(...)"
    severity: WARNING
