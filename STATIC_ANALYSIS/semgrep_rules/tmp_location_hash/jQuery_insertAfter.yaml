rules:
  - id: location_hash-jQuery_insertAfter
    languages: [javascript]
    message: "Source:location.hash;Sink:jQuery.insertAfter"
    mode: taint
    pattern-sources:
      - pattern: location.hash
      - pattern: $X = location.hash
    pattern-sinks:
      - pattern: "$(<... location.hash ...>).insertAfter(...)"
      - pattern: "$($X).insertAfter(...)"
    severity: WARNING
