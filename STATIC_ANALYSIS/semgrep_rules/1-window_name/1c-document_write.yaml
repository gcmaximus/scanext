rules:
  - id: 1c-document_write
    message: |
      "Source:window.name;Sink:document.write;
      window.name is vulnerable, which flows to innerHTML"
    languages:
      - javascript
    pattern-either:
      - pattern: |
          $X = window.name
          ...
          document.write(<...$X...>)
      - pattern: |
          $X = window.name
          ...
          function $FUNC(...,$ARG,...) {
            ...
            document.write(<...$ARG...>)
            ...
          }
          ...
          $FUNC(...,$X,...)
    severity: WARNING
