rules:
  - id: 1b-outerHTML.yaml
    languages: [javascript]
    message: "Source:window.name;Sink:outerHTML"
    mode: taint
    pattern-sources:
      - pattern: window.name
    pattern-sinks:
      - pattern: $OBJECT.outerHTML = ...
    severity: WARNING