rules:
  - id: chrome_runtime_onConnectExternal-jQuery_appendTo
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onConnectExternal.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onConnectExternal.addListener($ANY)
              - pattern: |
                  chrome.runtime.onConnectExternal.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - patterns:
          - pattern: "$($X).appendTo(...)"
          - focus-metavariable: $X
    message: Source:chrome_runtime_onConnectExternal;Sink:jQuery_appendTo
    languages:
      - js
    severity: WARNING
