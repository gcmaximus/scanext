rules:
  - id: chrome_runtime_onConnectExternal-jQuery_wrapAll
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onConnectExternal.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onConnectExternal.addListener($ANY)
              - pattern: |
                  chrome.runtime.onConnectExternal.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - patterns:
          - pattern: $(...).wrapAll($X)
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = $(...)
              ...
              $Y.wrapAll($X)
          - focus-metavariable: $X
    message: Source:chrome_runtime_onConnectExternal;Sink:jQuery_wrapAll
    languages:
      - js
    severity: WARNING
