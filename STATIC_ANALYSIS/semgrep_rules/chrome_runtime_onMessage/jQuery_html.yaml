rules:
  - id: chrome_runtime_onMessage-jQuery_html
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .html(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery.html
    languages:
      - js
    severity: WARNING
