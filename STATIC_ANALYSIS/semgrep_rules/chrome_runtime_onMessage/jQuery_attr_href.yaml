rules:
  - id: chrome_runtime_onMessage-jQuery_attr_href
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .attr('href',...);
    message: Source:chrome.runtime.onMessage;Sink:jQuery_attr_href
    languages:
      - js
    severity: WARNING
