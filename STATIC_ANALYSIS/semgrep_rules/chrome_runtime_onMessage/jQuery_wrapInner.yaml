rules:
  - id: chrome_runtime_onMessage-jQuery_wrapInner
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-inside: |
              chrome.runtime.onMessage.addListener(function( ... ) {
              ... 
              });
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .wrapInner(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery.wrapInner
    languages:
      - js
    severity: WARNING
