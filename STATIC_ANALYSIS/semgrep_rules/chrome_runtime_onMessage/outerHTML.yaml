rules:
  - id: chrome_runtime_onMessage-outerHTML
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - pattern: $OBJ.outerHTML = ...
    message: Source:chrome.runtime.onMessage;Sink:outerHTML
    languages:
      - js
    severity: WARNING
