rules:
  - id: chrome_runtime_onMessage-jQuery_globalEval
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: |
                  globalEval(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery_globalEval
    languages:
      - js
    severity: WARNING
