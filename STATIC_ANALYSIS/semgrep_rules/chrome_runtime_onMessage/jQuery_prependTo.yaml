rules:
  - id: chrome_runtime_onMessage-jQuery_prependTo
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .prependTo(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery.prependTo
    languages:
      - js
    severity: WARNING
