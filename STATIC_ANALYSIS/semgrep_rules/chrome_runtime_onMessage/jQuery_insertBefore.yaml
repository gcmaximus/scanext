rules:
  - id: chrome_runtime_onMessage-jQuery_insertBefore
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .insertBefore(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery.insertBefore
    languages:
      - js
    severity: WARNING
