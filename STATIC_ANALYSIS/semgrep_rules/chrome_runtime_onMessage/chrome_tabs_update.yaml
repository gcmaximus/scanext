rules:
  - id: chrome_runtime_onMessage-chrome_tabs_update
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onMessage.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onMessage.addListener($ANY)
              - pattern: |
                  chrome.runtime.onMessage.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.tabs.update(..., { ... });
    message: Source:chrome.runtime.onMessage;Sink:jQuery.chrome.tabs.update
    languages:
      - js
    severity: WARNING
