rules:
  - id: chrome_runtime_onMessage-jQuery_appendTo
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .appendTo(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery.appendTo
    languages:
      - js
    severity: WARNING
