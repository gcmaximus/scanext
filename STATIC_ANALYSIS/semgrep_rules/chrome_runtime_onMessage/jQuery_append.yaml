rules:
  - id: chrome_runtime_onMessage-jQuery_append
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onMessage.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onMessage.addListener($ANY)
              - pattern: |
                  chrome.runtime.onMessage.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern: $(...).append($X)
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = $(...)
              ...
              $Y.append($X)
          - focus-metavariable: $X
    message: Source:chrome_runtime_onMessage;Sink:jQuery_append
    languages:
      - js
    severity: WARNING
