rules:
  - id: chrome_runtime_onMessage-document_write
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: document.write($X)
              - pattern: ... .document.write($X)
    message: Source:chrome.runtime.onMessage;Sink:document.write
    languages:
      - js
    severity: WARNING