rules:
  - id: chrome_runtime_onMessage-jQuery_wrap
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-inside: |
              chrome.runtime.onMessage.addListener(function( ... ) {
              ... 
              });
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .wrap(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery.wrap
    languages:
      - js
    severity: WARNING
