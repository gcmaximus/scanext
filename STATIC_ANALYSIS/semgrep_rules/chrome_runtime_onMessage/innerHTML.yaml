rules:
  - id: chrome_runtime_onMessage-innerHTML
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessage.addListener(function($X, ...) {
              ... 
              });
          - focus-metavariable: $X
    pattern-sinks:
      - pattern: $OBJ.innerHTML = ...
    message: Source:chrome.runtime.onMessage;Sink:innerHTML
    languages:
      - js
    severity: WARNING
