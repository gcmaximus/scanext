rules:
  - id: chrome_runtime_onMessage-jQuery_insertAfter
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-inside: |
              chrome.runtime.onMessage.addListener(function( ... ) {
              ... 
              });
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: ... .insertAfter(...)
    message: Source:chrome.runtime.onMessage;Sink:jQuery.insertAfter
    languages:
      - js
    severity: WARNING
