rules:
  - id: chrome_runtime_onConnect-jQuery_html
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onConnect.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onConnect.addListener($ANY)
              - pattern: |
                  chrome.runtime.onConnect.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - patterns:
          - pattern: $(...).html($X)
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = $(...)
              ...
              $Y.html($X)
          - focus-metavariable: $X
    message: Source:chrome_runtime_onConnect;Sink:jQuery_html
    languages:
      - js
    severity: WARNING
