rules:
  - id: chrome_runtime_onConnect-innerHTML
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onConnect.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onConnect.addListener($ANY)
              - pattern: |
                  chrome.runtime.onConnect.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: |
                  $OBJ.innerHTML = ...
    message: Source:chrome_runtime_onConnect;Sink:innerHTML
    languages:
      - js
    severity: WARNING
