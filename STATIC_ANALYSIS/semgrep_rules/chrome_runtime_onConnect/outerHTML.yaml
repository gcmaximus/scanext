rules:
  - id: chrome_runtime_onConnect-outerHTML
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onConnect.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onConnect.addListener($ANY)
              - pattern: |
                  chrome.runtime.onConnect.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - pattern: $OBJ.outerHTML = ...
    message: Source:chrome_runtime_onConnect;Sink:outerHTML
    languages:
      - js
    severity: WARNING
