rules:
  - id: chrome_runtime_onConnect-jQuery_insertAfter
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onConnect.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onConnect.addListener($ANY)
              - pattern: |
                  chrome.runtime.onConnect.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
    pattern-sinks:
      - patterns:
          - pattern: $($X).insertAfter(...)
          - focus-metavariable: $X
    message: Source:chrome_runtime_onConnect;Sink:jQuery_insertAfter
    languages:
      - js
    severity: WARNING
