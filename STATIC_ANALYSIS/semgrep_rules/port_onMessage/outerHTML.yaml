rules:
  - id: port_onMessage-outerHTML
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  $ANYTHING.onMessage.addListener(function($X, ...) {
                    if($X.$Z === $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener(function($X, ...) {
                    if($X.$Z == $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener(function($X, ...) {
                    if($X === $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener(function($X, ...) {
                    if($X == $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener($X => {
                    if($X.$Z === $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener($X => {
                    if($X.$Z == $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener($X => {
                    if($X === $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener($X => {
                    if($X == $PASSWORD){
                      ...
                    }
                  ... 
                  });
              - pattern: |
                  $ANYTHING.onMessage.addListener($X => {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    if($X.$Z === $PASSWORD){
                      ...
                    }
                    ...
                  }
                  ...
                  $ANYTHING.onMessage.addListener($ANY)
              - pattern: |
                  function $ANY($X,...){
                    if($X.$Z == $PASSWORD){
                      ...
                    }
                    ...
                  }
                  ...
                  $ANYTHING.onMessage.addListener($ANY)
              - pattern: |
                  function $ANY($X,...){
                    if($X === $PASSWORD){
                      ...
                    }
                    ...
                  }
                  ...
                  $ANYTHING.onMessage.addListener($ANY)
              - pattern: |
                  function $ANY($X,...){
                    if($X == $PASSWORD){
                      ...
                    }
                    ...
                  }
                  ...
                  $ANYTHING.onMessage.addListener($ANY)
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  $ANYTHING.onMessage.addListener($ANY)
              - pattern: |
                  $ANYTHING.onMessage.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    if($X.$Z === $PASSWORD){
                      ...
                    }
                    ...
                  }
              - pattern: |
                  $ANYTHING.onMessage.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    if($X.$Z == $PASSWORD){
                      ...
                    }
                    ...
                  }
              - pattern: |
                  $ANYTHING.onMessage.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    if($X === $PASSWORD){
                      ...
                    }
                    ...
                  }
              - pattern: |
                  $ANYTHING.onMessage.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    if($X == $PASSWORD){
                      ...
                    }
                    ...
                  }
              - pattern: |
                  $ANYTHING.onMessage.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern: document. ... .outerHTML = $X
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = document. ...
              ...
              $Y. ... .outerHTML = $X
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              document. ...[$INT].outerHTML =$X
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = document. ...[$INT]
              ...
              $Y. ... .outerHTML = $X
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = document. ...
              ...
              $Y[$INT]. ... .outerHTML = $X
          - focus-metavariable: $X
    message: Source:port_onMessage;Sink:outerHTML
    languages:
      - js
    severity: WARNING
