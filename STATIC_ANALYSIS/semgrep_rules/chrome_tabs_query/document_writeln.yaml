rules:
  - id: chrome_tabs_query-document_writeln
    languages: [javascript]
    message: "Source:chrome_tabs_query;Sink:document_writeln"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query({...})
    pattern-sinks:
      - pattern: "document.writeln($X)"
      - patterns:
        - pattern: "... .document.writeln($X)"
        - focus-metavariable: $X
    severity: WARNING