rules:
  - id: chrome_tabs_query-jQuery_globalEval
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:jQuery.globalEval"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
        - pattern: "jQuery.globalEval($X)"
        - focus-metavariable: $X
    severity: WARNING