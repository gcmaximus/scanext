rules:
  - id: chrome_tabs_query-jQuery_prepend
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:jQuery.prepend"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
        - pattern: "$(...).prepend($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.prepend($X)
        - focus-metavariable: $X
    severity: WARNING
