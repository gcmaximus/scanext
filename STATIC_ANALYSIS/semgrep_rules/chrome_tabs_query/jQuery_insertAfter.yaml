rules:
  - id: chrome_tabs_query-jQuery_insertAfter
    languages: [javascript]
    message: "Source:chrome_tabs_query;Sink:jQuery_insertAfter"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query(...)
    pattern-sinks:
      - patterns:
        - pattern: "$($X).insertAfter(...)"
        - focus-metavariable: $X
    severity: WARNING
