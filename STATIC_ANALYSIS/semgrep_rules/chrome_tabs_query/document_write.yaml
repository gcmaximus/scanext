rules:
  - id: chrome_tabs_query-document_write
    languages: [javascript]
    message: "Source:chrome_tabs_query;Sink:document_write"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query(...)
    pattern-sinks:
      - pattern: "document.write($X)"
      - patterns:
        - pattern: "... .document.write($X)"
        - focus-metavariable: $X
    severity: WARNING