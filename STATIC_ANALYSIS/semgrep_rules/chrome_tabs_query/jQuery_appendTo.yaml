rules:
  - id: chrome_tabs_query-jQuery_appendTo
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:jQuery.appendTo"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
        - pattern: "$($X).appendTo(...)"
        - focus-metavariable: $X
    severity: WARNING
