rules:
  - id: chrome_tabs_query-setAttribute_src
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:setAttribute('src')"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
        - pattern: "document. ... .setAttribute('src', $X)"
        - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = document. ...
              ...
              $Y. ... .setAttribute('src', $X)
          - focus-metavariable: $X
    severity: WARNING
