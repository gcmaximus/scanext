rules:
  - id: chrome_tabs_query-outerHTML
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:outerHTML"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
          - pattern: document. ... .outerHTML = $X
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = document. ...
              ...
              $Y. ... .outerHTML = $X
          - focus-metavariable: $X
    severity: WARNING