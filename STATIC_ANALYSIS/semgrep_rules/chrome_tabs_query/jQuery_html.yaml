rules:
  - id: chrome_tabs_query-jQuery_html
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:jQuery.html"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
        - pattern: "$(...).html($X)"
        - focus-metavariable: $X
      - patterns:
        - pattern: |
            $Y = $(...)
            ...
            $Y.html($X)
        - focus-metavariable: $X
    severity: WARNING