rules:
  - id: chrome_tabs_query-eval
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:eval"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
        - pattern: "eval($X)"
        - focus-metavariable: $X
    severity: WARNING