rules:
  - id: chrome_tabs_query-jQuery_prependTo
    languages: [javascript]
    message: "Source:chrome.tabs.query;Sink:jQuery.prependTo"
    mode: taint
    pattern-sources:
      - pattern: chrome.tabs.query
    pattern-sinks:
      - patterns:
        - pattern: "$($X).prependTo(...)"
        - focus-metavariable: $X
    severity: WARNING
