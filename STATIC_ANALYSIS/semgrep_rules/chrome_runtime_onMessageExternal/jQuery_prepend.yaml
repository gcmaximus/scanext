rules:
  - id: chrome_runtime_onMessageExternal-jQuery_prepend
    mode: taint
    pattern-sources:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.runtime.onMessageExternal.addListener(function($X, ...) {
                  ... 
                  });
              - pattern: |
                  function $ANY($X,...){
                    ...
                  }
                  ...
                  chrome.runtime.onMessageExternal.addListener($ANY)
              - pattern: |
                  chrome.runtime.onMessageExternal.addListener($ANY)
                  ...
                  function $ANY($X,...){
                    ...
                  }
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern: $(...).prepend($X)
          - focus-metavariable: $X
      - patterns:
          - pattern: |
              $Y = $(...)
              ...
              $Y.prepend($X)
          - focus-metavariable: $X
    message: Source:chrome_runtime_onMessageExternal;Sink:jQuery_prepend
    languages:
      - js
    severity: WARNING
