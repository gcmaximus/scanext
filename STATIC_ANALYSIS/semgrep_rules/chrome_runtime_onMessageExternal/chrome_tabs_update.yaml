rules:
  - id: chrome_runtime_onMessageExternal-chrome_tabs_update
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: |
              chrome.runtime.onMessageExternal.addListener(function($X, ...) {
              ... 
              });
          - pattern: |
              function $ANY($X,...){
                ...
              }
              ...
              chrome.runtime.onMessageExternal.addListener($ANY)
          - pattern: |
              chrome.runtime.onMessageExternal.addListener($ANY)
              ...
              function $ANY($X,...){
                ...
              }
          - focus-metavariable: $X
    pattern-sinks:
      - patterns:
          - pattern-either:
              - pattern: |
                  chrome.tabs.update($X, { ... });
    message: Source:chrome_runtime_onMessageExternal;Sink:jQuery_chrome_tabs_update
    languages:
      - js
    severity: WARNING
